# 本工程任务的守护 JVM：detekt 1.23 内嵌 Kotlin 1.9 编译器无法在 JDK 25 上运行
# （IntelliJ JavaVersion 解析器拒绝 25.x 版本串），故固定 JDK 21；理由同 build.gradle.kts。
# 根构建以 include 方式使用本工程时不适用（同一守护进程），仅独立调用本工程时生效。
toolchainVersion=21
