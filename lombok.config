# Lombok 全局配置（仓库根，向下冒泡至各模块）。
# 让 Lombok 在生成的方法/构造器上加 @lombok.Generated 注解，使 SpotBugs / JaCoCo 等
# 静态分析与覆盖率工具自动跳过生成代码（避免对 @Value/@EqualsAndHashCode 等生成的
# equals/hashCode 误报冗余空检查 RCN、暴露内部引用 EI 等）。static-analysis.md：Lombok 模块需配套。
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
