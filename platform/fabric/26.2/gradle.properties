# 车道级平台/链路声明由本文件承担（Gradle 项目级属性作用域）。
# 构建级键（org.gradle.*）在子模块粒度不生效，统一由根 gradle.properties 决定（ADR-0026）。

# MC 26.2 为 Mojang 无混淆原始命名（ADR-0022）：告知 Loom 本版本无混淆，
# 跳过 mappings 装配并级联关闭 remap（不声明任何 mappings，与 ADR-0022 一致）。
# 26.2 车道的守护 JVM 由根构建统一要求 ≥ 25（ADR-0026 决策 4）。
fabric.loom.disableObfuscation=true
