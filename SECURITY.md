# 安全说明：MultiPlatformModTemplate

> 信任模型、敏感数据处理、外部输入校验与漏洞报告渠道。安全模型变化时更新。

## 1. 信任模型

- **客户端不可信**：服务端必须把来自客户端的所有协议包（C2S）当作**不可信外部输入**——校验合法性、范围、权限后再处理，禁止据 C2S 内容直接执行特权操作。
- **服务端相对可信**：客户端按协议处理 S2C 包，但仍须对越界 / 畸形数据做防御性处理，不致崩溃。
- **信任边界**在跨端协议层（`protocol`）与各平台的网络通道适配（`TransportPort` 实现）；这是主要攻击面。
- 作为脚手架 / 模板，MPMT 不引入额外网络监听端口，复用宿主平台的通信通道。

## 2. 密钥与敏感数据

- **严禁硬编码**任何密钥 / 令牌 / 凭据 / 内网地址到代码、注释、日志、提交信息（见全局安全准则与 `static-analysis` 依赖漏洞要求）。
- 配置中的敏感项走环境变量或宿主配置，仓库只放占位；`.env` 已被 `.gitignore` 排除。
- 日志脱敏：不把玩家隐私数据 / 完整凭据输出到日志或错误信息。

## 3. 外部输入

- 协议包反序列化：对长度 / 范围 / 枚举值 / 版本做校验；非法 / 截断输入明确拒绝，不崩溃、不静默错乱。
- 版本协商：不兼容的对端明确拒绝（防降级攻击 / 错乱）。
- 玩法逻辑（L0）对外部驱动的输入同样做领域级校验，不假设上游已校验。
- 客户端标识上报（规划功能，见 [`docs/specs/network-handshake-machine-code-ban.md`](docs/specs/network-handshake-machine-code-ban.md)）：上报的是**弱客户端标识**——不可信、**可伪造、可缺席**（不发包即回落原版玩家路径、绕过封禁）；沙箱 Java mod 取不到稳定硬件指纹（MAC 常被 OS 随机化、磁盘/主板序列号取不到），故**同机可能变、异机可能撞**。服务端只存其哈希、不据其执行特权动作；**身份真源以平台玩家 UUID 为准**、该标识仅作附加弱标签。封禁为**威慑、非安全保证**，强约束需叠加服务端身份（白名单 / 登录插件）。隐私：仅上报哈希、原始信息不出本机；采集器 `MachineCodeProvider` 可插拔以便合规替换。

## 4. 漏洞报告

- 发现安全问题请通过仓库的私有渠道报告（如 GitHub Security Advisory / 维护者邮箱），**不要公开 issue 暴露细节**。
- 响应预期：维护者确认后评估影响、修复并在补丁版发布（走 `sdd-hotfix`），必要时在 CHANGELOG 标注（不泄露可被利用的细节）。
