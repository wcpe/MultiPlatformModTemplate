# 变更日志

本项目所有重要变更记录于此。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布版本

### 新增
- 落地 **Sponge 平台基座骨架（工具链 + SPI 装配，FR-14 起步）**：新增独立 includeBuild `platform-sponge`——SpongeGradle 2.3.0（`org.spongepowered.gradle.plugin`）+ SpongeAPI `11.0.0-SNAPSHOT`，锚点 **MC 1.20.1·SpongeVanilla**。**按各 loader 最低 JDK 编译为 Java 21**（ADR-0004）——SpongeAPI 11 的 release `11.0.0` 与最新 `-SNAPSHOT` 制品均已要 Java 21、SpongeVanilla 1.20.1 最新 RC 随之；shade 进来的 core 为 Java 8 字节码、21 运行期兼容。`MpmtSpongePlugin`（`@Plugin("mpmt")` 入口 + Guice `@Inject` 注入 log4j2 Logger，`@Listener onConstruct(ConstructPluginEvent)` 经插件类加载器 ServiceLoader boot 装配 + 启用 + 记活跃平台）+ `SpongePlatformBootstrap`（SPI，platformId `sponge`）+ `SpongeFeatureGate`（纯服务端独立平台、能力位全否）；`sponge{}` DSL（`apiVersion`/`loader(PluginLoaders.JAVA_PLAIN)`/`plugin("mpmt"){entrypoint...}`）**生成插件元数据，不手写 `META-INF/sponge_plugins.json`**；core shadow shade + relocate snakeyaml（ADR-0012），spongeapi 由 `apiVersion` 接入（compileOnly、不 shade）。**Sponge 为纯服务端**（SpongeVanilla 无客户端插件 API）：FR-27 HUD 由 Sponge 服下发、realserver 用真实 SpongeVanilla 服 + 复用我方 Fabric 客户端伴侣（同 Bukkit 模式，不需 dev↔dev）。**验证**：编译（against spongeapi 11）+ 纯 JVM 装配测试 2 例（ServiceLoader 发现 sponge 入口 / FeatureGate 非 Folia）+ shadowJar 打包（jar 内含 `sponge_plugins.json` + 核心 shade + snakeyaml relocate 到 libs.* + 入口类）通过。传输（`RawDataChannel`）/ FR-26 能力 / acceptance / 真实服 realserver 随后续里程碑。
- 达成 **NeoForge realserver 端到端 `RESULT PASS`（沙箱实跑，FR-23）**：经**真实专用服绕开** NeoGradle 7 同项目并发 `ng_dummy` 墙——`neoforge-installer --installServer` 装专用服（`java @win_args.txt` 启动、非 gradle、无 ng_dummy；mods 放产品 shadowJar（含 core，生产服不需 coreLibJar）+ 验收 jar、`user_jvm_args.txt` 加 `-Dmpmt.acceptance` 激活），客户端仍用 dev `runClient`（Mojmap），**两端 Mojmap 故 FML 握手兼容**（NeoForge 无 SRG，区别于 Forge dev↔生产不兼容）。实跑：客户端进世界→上报 ClientReady→`NeoForgeSmokeServerScenario` 经产品通道发 ACTIONBAR HUD→`NeoForgeHudRenderer` 渲染、`NeoForgeSmokeClientVerifier` 断言→服务端写权威 `RESULT PASS`→halt（`TOTAL 1 PASS 1 FAIL 0`）。**至此 NeoForge 全栈（FR-15/FR-20/FR-19/FR-26/FR-27/FR-23）端到端打通**；realserver 维度待用户实机最终确认。
- 接通 **NeoForge realserver dev run 编排基础设施 + 服务端 dev run 实证**（FR-15/FR-23）：`build.gradle.kts` 加 `coreLibJar`（shade core/spi + relocate snakeyaml、`FMLModType:GAMELIBRARY` manifest）放 `run-*/mods` 解 **NeoGradle dev classpath 墙**——FML 模块层不向 `modSource` 的 mod 暴露 includeBuild/库依赖（core 会 `NoClassDefFoundError`），`additionalRuntimeClasspath` 配置在 7.0.116 **不存在**、per-run `dependencies{runtime(...)}` 亦不解，唯 `FMLModType:GAMELIBRARY` jar 入 mods 让 FML 当 game library 暴露给 mod；产品类走 `modSource(main)`、验收驱动 jar 入 mods；run 配置加 `-Dmpmt.acceptance` 激活 + report/deadline。**服务端 `runServer` 实证跑通**：corelib 当 GAMELIBRARY 加载 → `活跃平台：neoforge` → 验收驱动就绪等客户端。**dev↔dev 完整 `RESULT PASS` 卡 NeoGradle 7 同项目并发 `ng_dummy` 写冲突硬限制**（runServer 跑着时 runClient 配置必 `Failed to write dummy data for client-extra`；独立 `-g GRADLE_USER_HOME`、独立 `--project-cache-dir` 实测均不解——dummy 写在项目工作目录本身），须经独立项目副本或真实生产服+客户端绕过 + 用户实机确认（realserver 维度）。
- 落地 **NeoForge realserver 验收 harness L3（FR-23，编译级）**：platform-neoforge 新增独立 `acceptance` 源集（不入产品 jar、单独 shade 打 `mpmt-acceptance-neoforge` 驱动 mod、**无 reobf**——NeoForge 官方 Mojmap）——`NeoForgeAcceptanceControlChannel`（**SimpleChannel** 控制通道 `mpmt-test:acceptance`，单一 RawControlMessage 透传 byte[]、consumerMainThread 按 `ctx.getSender()` 分流服务端/客户端、`sendToServer`/`setClientReceiver`，NeoForge↔NeoForge 无需 Mixin，区别于 Forge 的裸字节+Mixin 路由）+ `NeoForgeServerGameTestContext`（server.execute 切主线程）+ `MpmtNeoForgeAcceptanceMod`（`-Dmpmt.acceptance=true` 激活 / ServiceLoader 发现场景 / 驱动线程跑 `ServerGameTestRunner` / 写单一权威报告 / 看门狗绝对截止 + CAS 单次收尾 + 硬退）+ `NeoForgeSmokeServerScenario`（经活跃传输 Holder 发 ACTIONBAR HUD `验收HUD`）+ `NeoForgeAcceptanceClientCompanion`（`@OnlyIn(Dist.CLIENT)`：到主菜单程序化连入、逐 client tick 上报 ClientReady + 服务 RunStep、每 tick 释放被抓取鼠标光标）+ 验证器/Registry/上下文/Outcome/Step（镜像 Forge）；build.gradle.kts 加 acceptance 源集 + `acceptanceJar`（shade 仅 `:acceptance`、不打 protocol/core-domain 防 split-package、无 reobf）。**NeoForge 20.2 API 要点**：`ServerData(name, ip, ServerData.Type.OTHER)`（1.20.2，Forge 1.20.1 用 `(name,ip,boolean)`）；`@SubscribeEvent` = `net.neoforged.bus.api.SubscribeEvent`；`TickEvent.ClientTickEvent`/`PlayerEvent.PlayerLoggedOutEvent`/`ServerStartedEvent` Forge 系；pack_format 18（1.20.2）。**验证**：编译 + compileAcceptanceJava + acceptanceJar 打包（split-package 守卫成立：jar 不含 protocol/core-domain）+ 纯 JVM 装配测试通过；**dev↔dev 实跑 RESULT PASS 属 realserver 维度、待编排 + 用户实机确认**（NeoGradle dev run classpath 墙同 FG，用 additionalRuntimeClasspath / jar 暂存解）。
- 落地 **NeoForge 平台能力示例 L3（FR-26）+ 跨端 HUD 渲染 L3（FR-27）（编译级）**：platform-neoforge `capability` 包镜像 Fabric（FR-26）与 Forge SimpleChannel HUD（FR-27）——`NeoForgeSchedulerPort`（无 Folia：runForEntity/Location/Global→主线程 `server.execute`、runAsync→守护单线程池、runTimer→`TickEvent.ServerTickEvent` END 相驱动 + 可取消句柄）/ `NeoForgePersistencePort`（UTF-8 properties）/ `NeoForgeMessagePort`（按 UUID 取在线玩家 `sendSystemMessage`）/ `NeoForgeDataDirectoryPort`（`FMLPaths.CONFIGDIR`）+ `NeoForgeCapabilityBootstrap`（`ServerStartedEvent` 装配端口 + L0 `PlatformCapabilityExample`，桥接 `PlayerEvent.PlayerLoggedInEvent`/`PlayerLoggedOutEvent`→领域 `PlayerJoinedEvent`/`PlayerLeftEvent`）；`NeoForgeHudRenderer`（`@OnlyIn(Dist.CLIENT)` 按 HudKind 切渲染线程渲染 title/actionbar/toast/chat）+ `net.NeoForgeClientHudReceiver`（向 `NeoForgeServerTransport.setClientReceiver` 注入解码+渲染）+ `proxy.SidedProxy`/`ServerProxy`/`ClientProxy`（Dist 分流，客户端代理注册 HUD 收包）；`MpmtNeoForgeMod` 接线 capability 装配 + 分离代理。**NeoForge 20.2 API 要点**：事件总线 `net.neoforged.neoforge.common.NeoForge.EVENT_BUS` + `@SubscribeEvent`（Forge 系）；周期 tick 仍 `TickEvent.ServerTickEvent` 的 `Phase.END`（拆分 Pre/Post 属 20.4+）；玩家事件 `getEntity()` 返 `Player`（非 ServerPlayer）；`Dist`/`@OnlyIn` 在 `net.neoforged.api.distmarker`；HUD 客户端类（Minecraft/Gui/SystemToast）与 Forge 逐字节一致（官方 Mojmap）。**验证**：编译 + 纯 JVM 装配测试 2 例 + shadowJar 打包通过；服务端不静态引用客户端专有类（HUD 经 `@OnlyIn(CLIENT)` + ClientProxy 隔离）。运行期渲染 / 玩家欢迎属 realserver 维度（NeoForge↔NeoForge dev↔dev 随后续）。
- 落地 **NeoForge 服务端真实传输（FR-20）+ 服务端网络特性接入（FR-19）**：platform-neoforge 新增 `net` 包——`NeoForgeServerTransport`（NeoForge `SimpleChannel`，产品通道 `mpmt:main`，单一 RawMessage 透传 byte[]、consumerMainThread 按 `ctx.getSender()` 分流服务端/客户端收包）+ `NeoForgeConnectionHandle`（封装 `ServerPlayer`、UUID 相等）；`MpmtNeoForgeMod` 构造期注册 `TransportPort` + 登记复用的 `ServerNetworkFeature`（FR-19）+ 暴露活跃传输 Holder（供验收驱动发跨端字节）。**关键发现**：NeoForge **20.2.93 的网络仍是 Forge 系**（`NetworkRegistry`/`SimpleChannel`/`NetworkEvent`，新 payload 注册 API 属 20.3+），故传输与 Forge SimpleChannel 版近 1:1——仅包名 `net.minecraftforge.*`→`net.neoforged.*`、且 `consumerMainThread` 直传 `Context`（Forge 1.20.1 传 `Supplier`）；SimpleChannel 加帧字节只适用 **NeoForge↔NeoForge**（realserver 目标、无需 Mixin），NeoForge↔Bukkit 异构互通（FR-11②）随后续按 ADR-0018 裸字节+Mixin 补。**验证**：编译（against neoforge 20.2.93）+ 纯 JVM 装配测试 + shadowJar 打包（核心 shade、snakeyaml relocate 到 libs.*、mods.toml/services 在位）通过；运行期收发属 realserver 维度（NeoForge↔NeoForge dev↔dev 随后续里程碑）。
- 落地 **NeoForge 平台基座骨架（工具链 + SPI 装配）**（FR-15 起步）：新增独立 includeBuild `platform-neoforge`——NeoGradle 7.0.116 + `net.neoforged:neoforge:20.2.93`，锚点 **MC 1.20.2**（NeoForge 无 1.20.1，PRD §7）。**与 Forge 的关键差异**：NeoForge 运行期用官方 Mojmap、**无 SRG / reobf / refmap**（NeoGradle 无 `mappings`/`reobf` 块）；Mixin **内置**、经 `mods.toml` `[[mixins]]` 声明（无 MixinGradle）。`MpmtNeoForgeMod`（`@Mod` 入口、构造期 boot 装配 + 启用 + 记活跃平台）+ `NeoForgePlatformBootstrap`（SPI，platformId `neoforge`，经 `META-INF/services` 注册）+ `NeoForgeFeatureGate`（镜像 `ForgeFeatureGate`，FML 包名改 `net.neoforged.*`、非 NeoForge 环境 try/catch 兜底）+ `mods.toml`（依赖 `neoforge` 非 `forge`、MC `[1.20.2,1.21)`）+ pack.mcmeta；core 经 shadow shade + relocate snakeyaml（ADR-0012）。**验证**：首次 NeoGradle 构建解压反编译 MC 1.20.2 ~5.5min、`compileJava`/`compileTestJava` 通过 + 纯 JVM 装配测试 2 例（ServiceLoader 发现唯一 neoforge 入口 / FeatureGate 非 Folia 分流）通过。**注**：dev run classpath 墙（同 FG，shaded/includeBuild 库默认不在 dev 运行期可见，NeoGradle 无全局 `additionalRuntimeClasspath` 配置）留 realserver dev run 里程碑解决；真实 TransportPort（NeoForge `PayloadRegistrar`）/ FR-26 能力 / FR-27 HUD / realserver harness 随后续增量。
- 落地 **网络可靠性层装配：透明分片/重组 + 重连重同步接线**（FR-24）：把已有的 `Fragmenter`/`Reassembler`/`ResyncCoordinator` 逻辑装配进收发管线。`PacketDispatcher` 收发**透明分片/重组**——发送时编码超过平台单包上限（`TransportPort.maxPayloadSize()` 减分片头部预留）即经 `Fragmenter` 按 seqId 切片逐片发；接收时 `FragmentPacket` 经 `Reassembler` 重组（CRC32 校验 + 30s 超时清理残片）集齐后按原包重新入站路由；不超上限的小包原样单帧收发、行为不变（不回归 FR-19）。**重连重同步**经 `ResyncCoordinator` 接线：`ServerNetworkFeature` 装配服务端处理器，收 `ResyncRequest` 后据 `sinceRevision` 重发权威状态（脚手架以确认 `ServerMessage` 示意——真实玩法在此回调挂状态重发，脚手架永不交付具体玩法）；`ClientNetworkFeature` 暴露 `requestResync(sinceRevision)`，由平台在重连 / 重新握手后触发（与 `startHandshake` 同为平台驱动的连接生命周期钩子）。纯 JVM 单测：超上限包透明分片重组往返 / 小包不分片 / 客户端 requestResync 发包 / 服务端据修订号重发状态。
- 落地 **融合服（CatServer）适配设计：进程级单一活跃绑定 + 融合服感知钩子**（FR-25，ADR-0008）：`PlatformProvider` 增**进程级（跨类加载器）单一活跃绑定守卫**——本类静态 `instance` 只拦同类加载器重复 boot，融合服（Forge+Bukkit 同进程）上我方 Bukkit 插件与 Forge mod 各自类加载器、各有一份本类静态拦不住"我方多入口同时激活"，故再用 JVM 全局系统属性 `top.wcpe.mc.mpmt.active-platform` 跨类加载器把关，已有我方活跃绑定再激活第二入口即 `PlatformAssemblyException` 失败快（融合服上只装一个我方入口、绑定 Bukkit 家族为唯一活跃平台，不激活我方 Forge 入口）；新增 `deactivate()` 在 disable 释放绑定（清实例 + 系统属性）以支持同 JVM 重新启用（Bukkit `/reload`）。`MpmtBukkitPlugin` 入口探测 `FeatureGate.HYBRID_FORGE_BUKKIT`（Forge 标志类 `MinecraftForge`）即记融合服感知日志、并在 disable 调 `deactivate`。纯 JVM 单测 2 例穷举：进程级标记已置时再激活失败快（模拟融合服多入口）/ `deactivate` 后可重新激活。**实跑随 1.12.2 真实 CatServer 属 P2**；本期只做适配设计 + FeatureGate 钩子 + 不变量细化（守 scope-discipline）。
- 落地 **Folia 区域调度适配**（FR-13，ADR-0013 / ADR-0019）：platform-bukkit 新增 `FoliaSchedulerPort`（按归属把任务落到 Folia 区域 / 实体线程——`runForEntity`→`Entity#getScheduler`(EntityScheduler)、`runForLocation`→坐标所属 `RegionScheduler`、`runGlobal`/`runTimer`→`GlobalRegionScheduler`、`runAsync`→`AsyncScheduler`；找不到实体 / 世界则退回全局线程不丢任务）+ `BukkitSchedulers.create(plugin, featureGate)` 选用工厂（探测 `Capability.REGION_SCHEDULER`（Folia 标志类 `RegionizedServer`）→ 区域调度，否则 → 主线程 `BukkitSchedulerPort`）；`BukkitCapabilityBootstrap.register` 增传平台 `FeatureGate`、入口传 `PlatformProvider.get().featureGate()`。**编译基线由 spigot-api 改为 paper-api（compileOnly，ADR-0019）** 以接 Folia 统一调度 API——paper-api 是 spigot 超集，运行期仍 Bukkit 家族、paper-only 调用经 FeatureGate 门控，**仍单一构建、不为 Folia 拆模块**（守 ADR-0007）。MockBukkit 单测 2 例穷举**选用**（Folia→FoliaSchedulerPort / 非 Folia→BukkitSchedulerPort，构造期不触 Folia API 故两分支可测）；真实 Folia 区域调度行为属实机维度（MockBukkit 不支持区域调度，realserver 由用户在真实 Folia 服确认，ADR-0013：调度端口须按平台写契约测试）。
- 打通 **Forge 客户端 ↔ Bukkit/Paper 服异构互通：Forge 端改用裸字节 + Mixin 收包，取代 SimpleChannel**（FR-11② / FR-20 / FR-27，ADR-0018）：spike 实测定论——Forge 判定非 Forge 服为 vanilla 连接后会门控掉 modded 通道（SimpleChannel/EventNetworkChannel）入站派发，**Forge 客户端能发不能收 Bukkit/Paper 服的裸包**，故 SimpleChannel 只覆盖 Forge↔Forge。改为：发包走原版 `Clientbound/ServerboundCustomPayloadPacket` 裸字节；收包由 **Mixin** 拦原版 `ClientPacketListener`/`ServerGamePacketListenerImpl` 的 `handleCustomPayload`、经 `ForgeRawPayloadRouter` 切主线程分发——拦截挂原版包层、不看对端是否 Forge，**一套机制同时打通 Forge↔Forge 与 Forge↔Bukkit**，字节与 Bukkit/Fabric 一致（protocol 单源，ADR-0006）。另把产品通道注册为 **FML 握手标记**（`NetworkRegistry.newEventChannel`，仅参与握手、不用其监听），修复去掉注册通道后 Forge↔Forge 客户端按 vanilla 连接处理、登录超时断连的回归。Mixin 经 **MixinGradle** 接入（`org.spongepowered:mixingradle:0.7.+` + AP `mixin:0.8.5:processor`，写 `MixinConfigs` 清单 + 生成 refmap，`verifyPackaging` 校验配置/refmap 在产物内）；dev↔dev Mojmap 运行期经 `-Dmixin.env.disableRefMap=true` 按 Mojmap 名直解，生产真实 Forge 服（SRG）照常用 refmap。**沙箱端到端实跑 `RESULT PASS` 两套**：① Forge↔Forge（`runServer`+`runClient` dev↔dev，服务端经 Mixin 收 ClientReady/StepResult）；② **Forge 客户端 ↔ 真实 Paper 服**（产品+验收驱动插件，Forge 伴侣经 Mixin 收 Paper 裸 HUD/控制包并验证往返）。realserver 维度须用户本机最终确认。引入依赖：`org.spongepowered:mixin`（Forge 47 已捆绑 0.8.5，构建期 AP/MixinGradle）。
- 落地 **Forge realserver 验收 harness 端到端实跑通过（RESULT PASS）+ 产品传输改用 SimpleChannel**（FR-23 / FR-27 / FR-20 推进，ADR-0014）：把 `ForgeServerTransport` 与验收控制通道 `ForgeAcceptanceControlChannel` 从 `EventNetworkChannel`（裸 `CustomPayload`）改为 Forge 标准 `SimpleChannel`——**实测 EventNetworkChannel 对裸 `CustomPayload` 不触发其监听、收不到**，致控制往返（ClientReady/RunStep/StepResult）与产品 HUD 收包全部失效；`SimpleChannel` 的 `send`/`sendToServer`/`consumerMainThread` 双向可靠。Forge 服的对端只能是 Forge 客户端（vanilla/Fabric 过不了 FML 握手、本就连不进 Forge 服），帧字节两端一致不影响；Fabric 客户端的异构互通由 Bukkit/Paper、Fabric 服侧的裸字节通道承担（smoke ②）。`MpmtForgeMod` 暴露**活跃传输 Holder**（启动期一次性、只读），`ForgeSmokeServerScenario` 经其发 ACTIONBAR HUD（走真实产品 `SimpleChannel`、不再裸发 `ClientboundCustomPayloadPacket`，真正贯通 FR-27 产品收发链路）。**修严重 bug**：验收客户端伴侣进世界后每 tick 释放被抓取的鼠标光标，不再把用户鼠标拉进游戏窗口 / 抢占焦点。验收以 **FG dev↔dev** 跑（`runServer` + `runClient` 均 Mojmap 使 FML 握手兼容、shade 后 Mojmap jar 放进 `run-server/mods`·`run-client/mods` 绕过 FG6 classpath 墙）。**沙箱实跑 `RESULT PASS`**：Forge 客户端进世界→上报 `ClientReady`→服务端经产品 `SimpleChannel` 发 HUD→`ForgeHudRenderer` 渲染、验证器断言→服务端写权威 `RESULT PASS`→halt。realserver 维度须用户本机最终确认。
- 落地 **平台能力示例 Bukkit L3 实现**（FR-26 推进）：platform-bukkit 新增 `capability` 包——`BukkitDataDirectoryPort`（`plugin.getDataFolder()` 基目录）+ `BukkitPersistencePort`（命名空间 properties 文件、UTF-8、synchronized，镜像 Fabric）+ `BukkitMessagePort`（按 UUID `Bukkit.getPlayer` 找在线玩家发聊天消息）+ `BukkitSchedulerPort`（非 Folia 下按归属 `runForEntity`/`runForLocation`/`runGlobal` 统一落主线程 `runTask`、异步 `runTaskAsynchronously`、周期任务 `runTaskTimer` 句柄经 `BukkitTask#cancel` 释放，ADR-0013；Folia 区域调度属 FR-13 不在本期）；`BukkitCapabilityBootstrap` 在 `onEnable` 装配 L3 端口 + 把**同一份 L0** `PlatformCapabilityExample` 接入运行时自有 EventBus + 桥接 Bukkit 玩家进退事件（`PlayerJoinEvent`/`PlayerQuitEvent`→`PlayerJoined`/`LeftEvent`），与现有 `TransportPort`/`ServerNetworkFeature` 接线并存。MockBukkit 单测 2 例覆盖：玩家进服经桥接投递领域事件 → L0 示例经 `BukkitPersistencePort` 异步持久化首次加入时间 / 同一玩家再次进服不覆盖首次加入时间；真实 Paper 端到端为实机维度（留 realserver harness）。证脚手架"一份 L0 逻辑经端口在各平台一致运行"的 Bukkit 落地，与 Fabric 镜像同一份 L0。
- 落地 **Forge 客户端验收伴侣 + Forge 跨端 HUD 渲染（编译级）**（FR-27 / FR-23 推进，ADR-0014）：platform-forge `main` 新增客户端侧 HUD——`ForgeHudRenderer`（`@OnlyIn(Dist.CLIENT)`，收 S2C `ServerHudMessagePacket` 按 `HudKind` 在客户端线程渲染 title/actionbar/toast/chat——先记 `volatile lastRendered` 快照再 `Minecraft#execute` 切渲染线程，ADR-0013）+ `ForgeClientHudReceiver`（`@OnlyIn(Dist.CLIENT)`，在**同一**产品通道 `mpmt:main`（`ForgeServerTransport#channel()` 暴露）追加 `ClientCustomPayloadEvent` 监听、裸字节 `PacketCodec.decode`→HUD 包交渲染器）；`ClientProxy` 接产品通道、`init` 注册 HUD 收包（仅 `Dist.CLIENT` 实例化，客户端专有类不被服务端加载）。`acceptance` 源集新增**真正的 Forge 客户端伴侣**——`ForgeAcceptanceClientCompanion`（`@OnlyIn(Dist.CLIENT)`：在同一验收通道 `mpmt-test:acceptance`（`ForgeAcceptanceControlChannel#channel()` 暴露）追加 `ClientCustomPayloadEvent` 收 RunStep 入队、`ClientPlayerNetworkEvent.LoggingIn` 上报 `ClientReady`、逐 `ClientTickEvent` 找验证器轮询判定后经裸 `ServerboundCustomPayloadPacket` 回 `StepResult`，步骤 200 tick 超时即 FAIL，decode 容错）+ `ClientVerifier`/`ClientVerifierRegistry`（ServiceLoader 按 scenarioId 索引）/`VerifyStep`/`VerifyOutcome`/`RealServerClientContext`（镜像 Fabric 客户端 gametest 侧）+ `ForgeSmokeClientVerifier`（scenarioId `smoke`、步骤 `smoke-ready`：客户端进世界 + `ForgeHudRenderer.lastRendered()` 为 ACTIONBAR `验收HUD` → OK）+ `ForgeAcceptanceClientInit` 客户端激活接缝；`MpmtForgeAcceptanceMod` 在 `-Dmpmt.acceptance=true` + `FMLEnvironment.dist==Dist.CLIENT` 时注册伴侣。**改用真正 Forge 客户端伴侣的根因**：Forge mod 通道需 FML 握手，vanilla/Fabric 客户端不做、跨栈通道不通，故 Forge↔Forge 才可用。**验证（仅编译级）**：`compileJava` + `compileAcceptanceJava` + `build` + `reobfAcceptanceJar` 全绿、打包校验通过、验收 jar 含全部客户端类 + `ClientVerifier` services（protocol/HUD 渲染器不入验收 jar，防 split package、运行期由产品 mod 提供）。沙箱无 headless Forge 客户端，**realserver 实机（真实 Forge 客户端 ↔ Forge 服）由用户本机跑**。
- 落地 **Forge 服务端真实传输 + 服务端网络特性接入**（FR-20 / FR-19 推进）：platform-forge 新增 `net` 包——`ForgeServerTransport`（实现 L0 `TransportPort`，用 Forge `EventNetworkChannel`（`NetworkRegistry.newEventChannel`）+ 裸 `ClientboundCustomPayloadPacket` 收发产品通道 `mpmt:main` 的**裸字节**——刻意不用 `SimpleChannel`（其会在 payload 前加消息索引帧字节，与 Fabric/Bukkit 裸字节协议不兼容），接受任意对端版本以支持异构客户端互通）+ `ForgeConnectionHandle`（封装 `ServerPlayer`、UUID 相等）；`MpmtForgeMod` 构造期注册 `TransportPort` + 登记平台无关 `ServerNetworkFeature`（复用同一份握手 / 协商 / 收发装配，platform-forge 增依赖 core-server 并 shade）。1.20.1 单锚点 SimpleChannel API 无版本差异、不引入 vX_Y 子层。**验证**：编译 + 打包校验通过 + **真实 Forge 专用服实跑启动**（Forge installer 装服 → mod jar 入 mods/ → `活跃平台：forge`、`Done`、零异常、SimpleChannel 注册与特性启用无误）；完整字节往返留 realserver harness。Forge dev run 因 FG6/FML 模块层不向 mod 暴露库依赖而不可用，故 Forge 走真实服 + shaded jar 验证（同 Bukkit+Paper）。
- 落地 **Bukkit realserver 验收 harness 并实跑验证（含异构互通）**（FR-23 / FR-11② 推进）：Bukkit 无 GameTest，故以 realserver 为唯一实机验收形态——platform-bukkit 新增独立 `acceptance` 源集（不入产品 jar、单独打 `mpmt-acceptance` 插件，仅验收运行期放入服务端）：`BukkitServerGameTestContext`（`BukkitScheduler.runTask` 切主线程）+ `BukkitAcceptanceControlChannel`（插件消息 `mpmt-test:acceptance` ↔ 复用平台无关 `AcceptanceClient`）+ `MpmtBukkitAcceptancePlugin`（`-Dmpmt.acceptance=true` 激活 / `ServiceLoader` 发现场景 / 驱动线程跑 `ServerGameTestRunner` / 写单一权威报告 / 看门狗绝对截止 + CAS 单次收尾 + 硬退）+ `BukkitSmokeServerScenario`；平台无关 acceptance 核心（控制协议 / 协调 / Runner / 报告）原样复用。**客户端复用 Fabric 验收伴侣连入真实 Paper 服**——沙箱实跑：真实 Paper（产品插件 + 验收驱动插件）+ 真实 Fabric 客户端经真实网络，`acceptance/smoke` **RESULT PASS**（Fabric 客户端进 Paper 世界→伴侣 ClientReady→服务端经 `mpmt:main` 发 ACTIONBAR HUD→客户端 `FabricHudRenderer` 渲染并断言），**证 Fabric 客户端 ↔ Bukkit/Paper 服务端异构互通（FR-11②）成立**。
- 落地 **Bukkit 服务端真实传输 + 服务端网络特性接入**（FR-20 / FR-19 推进）：platform-bukkit 新增 `net` 包——`BukkitServerTransport`（实现 L0 `TransportPort`，用 Bukkit 插件消息 `Messenger` 收发产品通道 `mpmt:main`、与 Fabric 同通道支持异构客户端互通，单包上限取 `Messenger.MAX_MESSAGE_SIZE`，入站在主线程回上层）+ `BukkitConnectionHandle`（封装 `Player`、UUID 相等）；`MpmtBukkitPlugin` 入口注册 `TransportPort`（需 JavaPlugin 实例做插件消息，故在入口而非 SPI assemble）+ 登记平台无关的 `ServerNetworkFeature`（复用同一份握手 / 协商 / 收发装配，platform-bukkit 增依赖 core-server 并 shade）。1.20.1 单锚点插件消息无版本差异、不引入 vX_Y 子层。MockBukkit 单测 8 例覆盖：收发通道注册 / 网络线程收包回上层带连接句柄 / 错通道与无回调兜底 / 无连接发送拒绝 / 单包上限 + 插件启用端到端接线；真实 Paper 字节往返为实机维度（留 realserver harness）。
- 落地 **平台无关配置加载模块 `core-config`**（FR-29，共享 L1，纯 Java 8）：新增 `core-config` 模块把 YAML / JSON 配置文件加载为类型化模型——`ConfigFormat`（按扩展名判别格式、枚举多态消灭 if-else）+ `ConfigLoader` 契约 + `YamlConfigLoader`（snakeyaml，仅目标类型为根、字段访问映射）/ `JsonConfigLoader`（gson）+ `ConfigService` 门面（按扩展名或显式格式选 loader、UTF-8 读文件、失败统一抛 `ConfigLoadException`）。平台无关、客户端 / 服务端共用、零项目依赖；基目录解析仍由 `core-paths` + `DataDirectoryPort` 负责、调用方组合（ADR-0010）。新增依赖 snakeyaml 2.2 + gson 2.10.1（relocate 到 `top.wcpe.mc.mpmt.libs.*` 是各平台 shade 期职责，ADR-0012）。纯 JVM 单测 18 例覆盖：格式判别边界 / YAML·JSON 往返加载 / 非法内容兜底 / 文件缺失 / 显式格式覆盖。
- 落地 **跨端 HUD 渲染 Fabric L3 并实跑验证**（FR-27 推进）：core-client `ClientNetworkFeature` 暴露 `dispatcher()` 供平台注册收包处理器；platform-fabric 新增 `FabricHudRenderer`（`@Environment(CLIENT)`，收 S2C `ServerHudMessagePacket` 按 `HudKind` 在客户端线程渲染 title/actionbar/toast/chat——先记快照再 `Minecraft#execute` 切渲染线程，ADR-0013），客户端入口注册到收发管线；`FabricNetworkBindings` 暴露产品通道 `PRODUCT_CHANNEL` 单一真源。realserver 冒烟场景扩展为**服务端经产品通道发 ACTIONBAR HUD → 客户端 `FabricHudRenderer` 渲染并记录 → 客户端验证器断言收到**（沙箱实跑 `RESULT PASS`）。
- 落地 **平台能力示例 Fabric L3 实现并实跑验证**（FR-26 推进）：platform-fabric 新增 `capability` 包——`FabricDataDirectoryPort`（configDir/mpmt 基目录）+ `FabricPersistencePort`（命名空间 properties 文件、UTF-8、synchronized）+ `FabricMessagePort`（按 UUID 找在线玩家发系统消息）+ `FabricSchedulerPort`（按归属统一落主线程 `server.execute`、异步守护线程池、tick 驱动可取消周期任务，ADR-0013）；`FabricCapabilityBootstrap` 在 `SERVER_STARTED` 装配 L3 端口 + 把**同一份 L0** `PlatformCapabilityExample` 接入自有 EventBus + 桥接 Fabric 玩家连接事件（`JOIN`/`DISCONNECT`→`PlayerJoined`/`LeftEvent`）。realserver 新增 `CapabilityServerScenario` **实跑验证**：玩家加入→桥接领域事件→L0 示例经 L3 端口异步持久化首次加入时间（`RESULT PASS`）。证脚手架"一份 L0 逻辑经端口在真实平台一致运行"成立。
- 落地 **网络可靠性层重连/重同步**（FR-24 推进，纯 JVM 平台无关）：protocol 新增 `ResyncRequestPacket`(C2S 0x82，sinceRevision:long) 注册进 PacketCodec 并纳入帧往返一致测试；`protocol.reliability` 新增 `ResyncCoordinator`（客户端 `requestResync(long)` 发请求；服务端注入 `ResyncRequestHandler` 收请求据修订号重发权威状态；收发收敛一处、线程安全依赖 PacketDispatcher）。纯 JVM 单测：帧往返 + 客户端请求发包 / 服务端收请求调处理器 / 入参校验。至此可靠性层分片(`Fragmenter`)/重组(`Reassembler`+CRC)/重连重同步(`ResyncCoordinator`)三件齐备。
- **模拟服 GameTest 套件落地并实跑通过（FR-23①，RESULT PASS）**：platform-fabric gametest 加 in-process 回环网络 GameTest——`LoopbackTransport`（进程内服务端↔客户端直连）+ `LoopbackHandshakeGameTest`（在真实 Fabric 服务端运行期跑通产品握手 + 版本协商 + 标识上报 + 欢迎建会话 + Ping/Pong 往返并逐项断言）+ `SimDriverBootstrap`（`-Dmpmt.simtest=true` 激活、`SERVER_STARTED` 跑 `ServerGameTestRunner`、写报告、halt）；Loom 运行配置 `runSimNetworkTest` + 一键门禁 `runSimNetworkAcceptance`（读报告末行 `RESULT PASS`）。**沙箱实跑：headless 服 Done 3.8s→回环握手 GameTest 8ms PASS→`RESULT PASS`→halt→门禁放行 BUILD SUCCESSFUL**。**至此 MVP 验收门 GameTest 两套（① 模拟服 + ② realserver）均实跑 `RESULT PASS`**（FR-23 / PRD §6）。
- **realserver 验收端到端实跑通过（服务端 + 客户端 RESULT PASS）**（FR-23 推进，ADR-0014）：沙箱内起真实 Fabric 1.20.1 专用服（`runAcceptanceServer`）+ 真实 Fabric 客户端（`runAcceptanceClient`，`--quickPlayMultiplayer 127.0.0.1` 自连）并发跑通——客户端加载 `mpmt-acceptance` 测试 mod→伴侣注册→`Player 进世界`→上报 `ClientReady`→服务端 `SmokeServerScenario.runClientStep("smoke-ready")`→`SmokeClientVerifier` 判 OK→服务端聚合权威报告 **`RESULT PASS`**→`halt`；`runRealServerAcceptance` 门禁读 `RESULT PASS` 放行（BUILD SUCCESSFUL）。**整套 server-drives/client-verifies/single-authoritative-report 验收链路运行期验证通过**。调优：`SmokeServerScenario` 客户端就绪超时放宽 180s（冷启动余量）、`acceptanceServer` 看门狗 300s、`acceptanceClient` 独立 runDir、服务端 offline-mode。realserver 维度（ADR-0014/FR-23②）达成；FR-23① 模拟服 GameTest 套件仍待落地。
- 落地 **realserver 验收 Gradle 起服编排 + 门禁，并实跑验证服务端侧**（FR-23 推进，ADR-0014）：platform-fabric 加 Loom 运行配置 `runAcceptanceServer`（服务端 headless）/`runAcceptanceClient`（客户端 `--quickPlayMultiplayer` 自连）+ `runRealServerAcceptance` 门禁任务（读权威报告末行 `RESULT PASS`，非则构建失败）；`AcceptanceDriverBootstrap` 写报告自动建父目录。**已实跑 `runAcceptanceServer`**：真实 Fabric 1.20.1 专用服 headless 启动（Done 19s）→驱动激活→`ServiceLoader` 发现 `SmokeServerScenario`→`awaitClientReady` 60s 超时（无客户端连入）→渲染权威报告 `RESULT FAIL`→`halt`，**全链路运行期验证通过**；`runRealServerAcceptance` 实跑验证正确拒绝 `RESULT FAIL`。客户端侧需显示、整套 `RESULT PASS` 须用户本机（`runAcceptanceClient` 连入）。
- 落地 **realserver 验收激活入口 + 绝对截止看门狗**（FR-23 推进，ADR-0014）：gametest 测试 mod `fabric.mod.json`（id `mpmt-acceptance`，main→`AcceptanceGametestInit` 服务端驱动 / client→`AcceptanceGametestClientInit` 客户端伴侣，depends mpmt+fabric-api）+ 两个入口类；`AcceptanceDriverBootstrap` 补**绝对截止看门狗**（`-Dmpmt.acceptance.deadlineMs`，到点写 fallback `RESULT FAIL` 报告并 `halt`，`finished` CAS 单次收尾防驱动/看门狗双写，硬退兜底 `Runtime.halt`）；`FabricAcceptanceControlChannel` 挂接 `ServerPlayConnectionEvents.DISCONNECT`→`failAllPending` 即时唤醒挂起步骤；`processGametestResources` 注入版本号。**编译期经真实 Fabric 1.20.1 验证、产品 jar 不变（391 条目）**。Gradle 一键起服编排 + 实跑属实机维度（须用户本机，客户端需显示）。
- 落地 **realserver 验收 Fabric 客户端验证伴侣**（FR-23 推进，ADR-0014，platform-fabric `gametest` 源集客户端侧）：`ClientVerifier`(scenarioId/poll→VerifyOutcome|null) + `VerifyStep` / `VerifyOutcome`（OK/FAIL/ERROR 工厂）+ `RealServerClientContext`（`@Environment(CLIENT)`，暴露 Minecraft 供验证器读客户端态）+ `ClientVerifierRegistry`（ServiceLoader 按 scenarioId 索引、重复即拒）+ `AcceptanceClientCompanion`（`@Environment(CLIENT)`：`ClientPlayNetworking` 收 RunStep 入队、进世界即上报 ClientReady、逐 client tick 找验证器轮询判定后回 StepResult，步骤 200 tick 超时即 FAIL，decode 容错）+ 示例 `SmokeClientVerifier`。控制通道 id 抽出共享 `AcceptanceControlChannelId`（服务端接入与客户端伴侣共用、防漂移）。复用 acceptance 模块的控制协议 codec（服务端 / 客户端单一真源、无双份字节对齐风险）。程序化连接由启动参数 `--quickPlayMultiplayer`（Round H）完成、伴侣不做。**编译期经真实 Fabric 1.20.1 验证、产品 jar 不变（391 条目）**。
- 落地 **realserver 验收 Fabric 服务端接入层**（FR-23 推进，ADR-0014，platform-fabric 新增 `gametest` 源集）：Loom `gametest` 独立源集（继承 main 的 MC + fabric-api 类路径 + acceptance 核心，**不入产品 jar**，build 纳入编译校验）；`FabricServerGameTestContext`（`server.execute` 切主线程实现 onMain/waitTicks/awaitUntil）；`FabricAcceptanceControlChannel`（经 `ServerPlayNetworking` 独立 test 通道 `mpmt-test:acceptance` 收发控制协议字节、桥接 `AcceptanceClient`，与产品通道分离）；`AcceptanceDriverBootstrap`（`-Dmpmt.acceptance=true` 激活、`SERVER_STARTED` 后经 `ServiceLoader` 发现并绑定场景、驱动线程跑 Runner、写权威报告、`server.halt`）；示例 `SmokeServerScenario`（经 `ServiceLoader` 发现）。**编译期经真实 Fabric 1.20.1 验证、产品 jar 不受影响（条目数不变）**；激活入口（gametest fabric.mod.json）+ Gradle 起服编排 + 客户端验证器属实机维度、待后续。
- 落地 **realserver 验收 harness 服务端 GameTest 框架**（FR-23 推进，ADR-0014，acceptance 模块）：平台无关——`ServerGameTest`(suite/id/run) + `ServerGameTestContext`（接口：平台实现 onMain/waitTicks/awaitUntil，默认方法 assertTrue/assertEquals/fail/skip）+ `ServerGameTestRegistry`（按 suite/id 去重 + 稳定排序）+ `ServerGameTestRunner`（顺序执行、归类 PASS/SKIP/FAIL/ERROR、**用例间隔离不互相中断**、聚合 `ScenarioResult`）+ `ServerScenario` 基类（经注入的 `AcceptanceClient` 提供 `awaitClientReady` / `runClientStep`，避免静态可变单例）。纯 JVM 单测 12 例（断言/跳过、注册去重排序、四态归类与隔离、场景排程与状态→断言映射）。平台上下文实现 / 控制通道 / 引导待 Fabric 接入。
- 落地 **realserver 验收 harness 平台无关核心**（FR-23 起步，ADR-0014，镜像 AllinCore-New）：新增不发布的 `acceptance` 测试设施模块（独立于产品协议、不入产品 jar）——测试控制协议 `ClientReadyPacket`(C2S)/`RunStepPacket`(S2C)/`StepResultPacket`(C2S) + `StepStatus`（稳定线缆码）+ 手写 `AcceptanceControlCodec`（int 长度前缀 UTF-8，非法/截断/未知类型即拒）；`AcceptanceClient` 客户端排程协调（seq→`CompletableFuture` 对账 + `CountDownLatch` 就绪门闩 + 单步超时 + `failAllPending` 收尾唤醒，线程安全）；单一权威报告 `AcceptanceReport`（`SERVER-GAMETEST-REPORT` + TOTAL 统计 + 末行 `RESULT PASS|FAIL`，SKIP 不计失败、空结果判失败）。纯 JVM 单测 30 例（codec 往返/字节稳定/边界 18、协调 seq/门闩/超时/收尾 6、报告聚合/判定 6）。Fabric 控制通道 / 场景 / 客户端验证器 + Gradle 起服编排属实机维度、待后续。
- 落地 **Fabric 客户端传输 + 客户端网络装配特性**（FR-19 推进，与服务端对称）：L1 core-client 新增 `ClientNetworkFeature`（实现 `Feature`，装配 `PacketDispatcher` + `HandshakeClientService`，平台无关、纯 JVM 单测 3 例覆盖发起握手发 ClientHello / 被接受后上报弱标识 / 收服务端消息 / 入参校验）；core-client 增依赖 core-runtime。platform-fabric 新增 L4 `FabricClientNetwork`(接口) + `v1_20.V1_20ClientNetwork`（`@Environment(CLIENT)`，1.20.1 `ClientPlayNetworking`）+ `FabricNetworkBindings.clientNetwork` 装配点 + L3 `FabricClientTransport`（`@Environment(CLIENT)`，无连接发送 + 服务端哨兵句柄）；版本探测抽出共享 `FabricVersions.detect()`（服务端入口同步改用）；`MpmtFabricClientBootstrap` 以独立客户端运行时装配本特性，连入服务端（`ClientPlayConnectionEvents.JOIN`）时发起握手；`fabric.mod.json` 声明依赖 fabric-api。core-client shade 进 Fabric 产物，编译 + 打包校验通过，运行期收发待 GameTest。
- 落地 **服务端网络装配特性 + Fabric 接入**（FR-19 推进）：新增 L1 core-server `ServerNetworkFeature`（实现 core-runtime `Feature`）——`onEnable` 取平台注入的 `TransportPort`，装配 `PacketDispatcher` + `HandshakeServerService` + `HudMessageService` + 示例 Ping→Pong；**平台无关，各平台注入自己的 `TransportPort` 即复用同一份服务端网络装配**（"逻辑写一次"）。core-server 增依赖 core-runtime（ARCHITECTURE §2.2）。Fabric 入口 `MpmtFabricBootstrap` 登记本特性，core-server shade 进 Fabric 产物。纯 JVM 单测 3 例（装配后握手 + 标识上报欢迎 + Ping/Pong 往返、装配产物启用前后可见性、入参校验）；Fabric 编译 + 打包校验通过，运行期收发待 GameTest。
- 落地 **Fabric L4 版本适配机制 + 服务端真实传输**（FR-10/FR-20 起步，platform-fabric）：新增 fabric-api 依赖（网络 API 来源）；L4 `version` 子层 `SupportedVersion`（运行期探测 MC 版本→选锚点 1.20.1，缺失即失败快，纯 JVM 单测 2 例）+ `FabricServerNetwork`(版本无关接口) + `v1_20.V1_20ServerNetwork`（用 1.20.1 `ServerPlayNetworking` + `ResourceLocation`(`mpmt:main`) + `FriendlyByteBuf` 实现裸字节收发，唯一引用版本 API 处）+ `FabricNetworkBindings`（按版本装配的唯一 switch 装配点）；L3 `FabricServerTransport`(实现 L0 `TransportPort`，委托 L4 绑定) + `FabricConnectionHandle`(封装 `ServerPlayer`、UUID 相等，平台原生对象不泄漏进 L0/L1)；`FabricPlatformBootstrap.assemble` 探测版本→装配绑定→注册 `TransportPort`。**编译期经真实 Fabric 1.20.1 + Mojmap 验证**；版本选择 + 装配缺运行时失败快有纯 JVM 单测；运行期收发与客户端方向（ClientPlayNetworking）待 GameTest / 后续。
- 落地 **跨端 HUD 消息 L1 层**（FR-27 起步，纯 JVM 平台无关）：protocol 新增 `ServerHudMessagePacket`(S2C 0x05) + `HudKind`（TITLE/ACTIONBAR/TOAST/CHAT，**稳定线缆码而非 ordinal**，未知码抛 `ProtocolException`），字节布局 `kind(VarInt)+text(UTF)+subtitle(UTF)+durationMillis(long)`，注册进 PacketCodec 并纳入帧往返一致测试；core-server 新增 `HudMessageService`（`send(ConnectionHandle, HudKind, text[, subtitle, durationMillis])` / `sendTitle`，经 PacketDispatcher 下发，入参非空校验）。纯 JVM 单测：HUD 字段级往返 + 线缆码边界 7 例、下发服务 4 例、并入帧往返样本 4 类。客户端各平台渲染（L3，渲染线程读不可变快照）与按玩家定位下发（整合会话注册表）待后续。
- 落地 **平台能力示例 L0 层**（FR-26 起步，纯 JVM 平台无关）：新增 L0 端口 `SchedulerPort`（按归属调度 `runForEntity`/`runForLocation`/`runGlobal`/`runAsync`/`runTimer`，实现 ADR-0013，触碰世界 / 实体态必经带归属入口、周期句柄可关闭）+ `PersistencePort`（namespace/key 字符串读写）+ `MessagePort`（向玩家发文本）；新增 L0 内核领域引用 `PlayerRef`/`EntityRef`/`WorldRef`（Lombok 值对象）；新增 `capability` 功能域：`PlayerJoinedEvent`/`PlayerLeftEvent`（经自有 EventBus 协作，ADR-0011）+ `PlatformCapabilityExample`（玩家加入→异步持久化首次加入时间→按归属发欢迎→注册周期心跳；离开→关闭心跳句柄释放资源；时钟可注入）。纯 JVM 单测 7 例（首次/再次加入、心跳周期、离开释放、重复加入防泄露、经 EventBus 订阅、入参校验）。各平台事件桥接 / 调度 / 持久化 L3 实现待后续，实机一致性维度待用户确认。
- 新增 **共享目录与资源路径模块**（FR-30，纯 JVM 平台无关，ADR-0010）：新增 L0 端口 `DataDirectoryPort`（`@FunctionalInterface`，平台提供基目录 `java.nio.file.Path baseDirectory()`，`Path` 为 JDK 标准类型守 L0 不依赖平台）+ L1 模块 `core-paths` 的 `ResourcePaths`（在基目录下预设 `config/`/`data/`/`resources/` 标准目录，`configFile`/`dataFile`/`resourceFile` 解析相对名，调用方引用预设不自算路径；相对名规范化后越界 / 绝对路径即拒、基目录为空失败快）。客户端 / 服务端共用同一份预设。纯 JVM 单测 8 例（预设位置 / 相对名解析 / 嵌套 / 越界拒绝 / 绝对路径拒绝 / 空白名 / 失败快 / 端口为空）。平台基目录实现待平台胶水按需提供。
- 新增 **会话与心跳逻辑**（FR-28 起步，纯 JVM 平台无关）：`SessionRegistry`（服务端在线会话登记 / 查询 / 下线 / 在线列表，线程安全；core-server 接入 Lombok 用于 Session 值对象）+ `RttTracker`（客户端心跳 RTT 计算 + 超时清扫判疑似丢失，时钟可注入）。纯 JVM 单测 7 例（会话 3 + RTT 4）。周期发包与重连重同步联动由上层 / 平台驱动（后续）。
- 新增 **协议包**（FR-21/22）：`ClientIdReportPacket`(C2S 0x81)、`ServerMessagePacket`(S2C 0x02)、`DisconnectPacket`(S2C 0x03)，注册进 PacketCodec、纳入往返一致测试。
- 新增 **L0 弱客户端标识与封禁域**（FR-21 / FR-22 起步）：`MachineCode`（Lombok 值对象，弱标识）+ `BanEntry` + `BanRegistry`（线程安全 ban/unban/isBanned/list，纯逻辑）+ `MachineCodeProvider` 端口（客户端侧弱标识提供者）。core-domain 接入 Lombok（首批领域值对象，ADR-0004/FR-01）。纯 JVM 单测 4 例穷举封禁表。
- 落地 **网络可靠性层·分片与重组**（FR-24 起步，L1 protocol·平台无关·线程安全）：`FragmentPacket`（id 0x10）+ `Fragmenter`（按上限切片、各片携带完整载荷 CRC32）+ `Reassembler`（按 seqId 归组、乱序可重组、CRC 校验、超时清理，时钟可注入）；codec 增加定长 `int`（4 字节大端，供 CRC32）。纯 JVM 单测 12 例（往返各尺寸 / 乱序 / 单片 / CRC 检出 / 超时清理）。（重连 / 重同步随会话特性后续。）
- 落地 **握手服务 + 跨端冒烟集成**（FR-03 / FR-11 / FR-21 起步）：
  - L0 `HandshakeStateMachine` 扩展标识上报迁移：`onClientId(banned)`（HELLO_OK → ESTABLISHED / REJECTED）。
  - L1 `core-server`：`HandshakeServerService`（收 ClientHello → 版本协商 → 回 ServerHello；收 ClientIdReport → 封禁校验 → 欢迎建会话 / 告知封禁并通知断开，真实踢出由平台 L3 调度执行）。
  - L1 `core-client`：`HandshakeClientService`（发 ClientHello、被接受后上报弱标识、接收服务端消息 / 断开通知，volatile 暴露结果）+ `DefaultMachineCodeProvider`（弱系统属性 SHA-256，原始来源可注入）。
  - `smoke`（不发布）：`InProcessLoopbackTransport`（进程内回环传输，FR-20）+ 集成测试，**纯 JVM 跑通"进服握手 + 版本协商 + 标识上报 + 封禁判定 + 一次往返包"全链路 + 不兼容版本被拒 + 被封禁被通知断开**（FR-11 ② 的逻辑证明；真实异构互通为实机维度）。
- 新增 **跨端收发核心**（FR-19 / FR-21 起步）：
  - L0：`TransportPort`（裸 `byte[]` 收发，不依赖协议层以守 L0⊄L1）+ `ConnectionHandle`（不透明连接句柄）+ `HandshakeStateMachine`（纯逻辑状态机 CONNECTED→HELLO_OK/REJECTED→ESTABLISHED，非法迁移失败快、版本兼容性由 L1 传入）。
  - L1 protocol：`PacketDispatcher` 收发管线——在 TransportPort 之上用 PacketCodec 编码发送 / 解码按 id 路由，非法 / 截断 / 未知字节不崩溃，无处理器静默忽略，处理器表并发安全。
  - 纯 JVM 单测：握手迁移 3 例 + 收发管线 4 例。
- 落地 **L3 platform-forge 平台胶水**（FR-09 起步，独立 includeBuild·ForgeGradle 6.0.54·Forge 1.20.1-47.4.2·官方映射）：`ForgePlatformBootstrap`（SPI + `META-INF/services` 注册）+ `ForgeFeatureGate`（Forge 无 Folia；同进程有 Bukkit 判融合服；客户端发行环境判集成服）+ `MpmtForgeMod`（`@Mod` 入口，构造期装配运行时）+ **client/server 分离代理**（`SidedProxy`/`ClientProxy`/`ServerProxy`，按 `FMLEnvironment.dist` 选择）。打包：shade platform-spi + core + relocate snakeyaml，再 **reobf 到 SRG** 供真实 Forge 运行；`verifyPackaging` 守护。已验证 **ForgeGradle 可在本仓库 Gradle 8.10 起来**；纯 JVM 测试 2 例验证经真实 ServiceLoader 发现 Forge 入口 + FeatureGate 分流；真实客户端 / 服务端为实机维度。
- 落地 **L3 platform-fabric 平台胶水**（FR-08 起步，由 M0 spike 载体升级为真正胶水）：`FabricPlatformBootstrap`（SPI + `META-INF/services` 注册）+ `FabricFeatureGate`（Fabric 无 Folia / 非融合服，集成服按发行环境探测，纯 JVM 下保守判否）+ **main/client 双端入口**（`MpmtFabricBootstrap` 两端共用、一次性装配运行时；`MpmtFabricClientBootstrap` 客户端接缝）。打包链路升级为 shade platform-spi + core 全链 + relocate snakeyaml 进 remapped jar（`verifyPackaging` 加查 platform-spi 在位）。纯 JVM 测试 2 例验证经真实 ServiceLoader 发现 Fabric 入口 + FeatureGate 分流；真实客户端 / GameTest 模拟服为实机维度（随网络 / smoke 特性落地）。
- 落地 **L3 platform-bukkit 平台胶水**（FR-07 起步，Bukkit 家族单一构建）：普通 Java + shadow，编译针对 **spigot-api 1.20.1** 基线（compileOnly），`BukkitPlatformBootstrap`（SPI + `META-INF/services` 注册）+ `BukkitFeatureGate`（按类存在探测 Folia / 融合服能力）+ `MpmtBukkitPlugin`（JavaPlugin 入口，进服后经本插件类加载器装配运行时）。打包：shade 共享核心 + relocate snakeyaml 进插件 jar（`verifyPackaging` 守护，**closes ADR-0012 Bukkit relocate 验证**）。集成测试用 **MockBukkit**（无真实服）验证装配链路：插件启用 → 平台装配为 bukkit → FeatureGate 分流（FR-23）。真实 Paper 服装配为实机维度（待用户确认）。
- 落地 **L2 platform-spi 平台抽象层**（FR-05 / FR-06 起步）：`PlatformBootstrap` SPI + `FeatureGate`（`supports(Capability)` 能力探测）+ `PlatformProvider`（Holder，一次性装配后只读，重复 boot 失败快）+ `PlatformAssembler`（`ServiceLoader` 发现唯一活跃平台，零 / 多入口启动期失败快）。新增 **ADR-0017**（平台发现 / 装配编排归属 L2、L1 不依赖 L2，细化 ADR-0002）。集成测试 6 例（含经 `META-INF/services` 注册的假平台被发现并把端口注入 L1 运行时）。
- 落地 **L1 core-runtime 框架编排**（FR-02 起步）：`MpmtRuntime` 生命周期（NEW→ENABLED→DISABLED，转换守护、非法转换失败快）+ `Feature` / `FeatureRegistry`（按序登记、按名去重、启用顺序 / 停用逆序）+ `RuntimePorts`（类型安全端口注册表，装配期写入、之后只读）+ `RuntimeContext`（向特性暴露 EventBus 与端口）。平台发现在 L2、注入本运行时，L1 不依赖 L2（ADR-0001）。纯 JVM 单测 8 例覆盖生命周期时序 / 守护 / 上下文装配 / 启用异常传播 / 端口注册边界。
- 落地 **L1 protocol 跨端协议骨架**（FR-04 起步）：`ProtocolVersion`（`CURRENT` / `MIN_SUPPORTED` + `isCompatible` 版本协商）；编解码原语 `ProtocolBufWriter` / `ProtocolBufReader` + `byte[]` 默认实现（字节布局与 MC 线缆对齐，非法 / 截断输入抛 `ProtocolException` 不崩溃）；`Packet` + `PacketCodec`（帧头 + 按 id 注册表分发）；握手包 `ClientHello` / `ServerHello` 与往返包 `Ping` / `Pong`。纯 JVM 单测 46 例覆盖往返一致 / 版本协商边界 / VarInt 与 long 边界 / 非法与截断输入（FR-04 / ADR-0006 / testing-and-quality §2）。
- 落地 **L0 自有 EventBus 内核**（FR-01 起步）：`EventBusPort`（订阅 / 发布）+ `DomainEvent` 标记接口 + `SimpleEventBus` 默认实现——平台无关、线程安全（`ConcurrentHashMap` + `CopyOnWriteArrayList`）、按精确类型分发、订阅者异常隔离、零第三方依赖（JDK 自带日志）。纯 JVM 单测覆盖正常 / 类型隔离 / 无订阅者 / 异常隔离 / null 校验 / 并发（FR-01 / ADR-0011 / testing-and-quality §2）。
- 落地 **M0 Gradle 复合构建骨架 + 打包 spike + 跨栈 spike**（`docs/specs/build-skeleton-and-spikes.md`）：
  - Gradle 8.10.2 wrapper + 根复合构建（Kotlin DSL）；`core-domain`（根常规模块，JDK 8 工具链，零第三方运行期依赖）+ `platform-fabric`（独立 includeBuild·Loom 1.7.4·MC 1.20.1·Mojang 官方映射·Java 17）。
  - **打包 spike 通过**：core 纯 Java 经 shadow shade 进 Fabric remapped jar 且不被 remap、snakeyaml relocate 到 `top.wcpe.mc.mpmt.libs.*`、`fabric.mod.json` 注入版本号；自动化校验任务 `verifyPackaging` 守护。**结论：core 消费走 includeBuild 依赖替换成立，未触发 ADR-0012 的 mavenLocal 回退。**
  - **跨栈 spike 通过**：自动化字节等价测试（12 例）证明 Fabric `FriendlyByteBuf` 与普通 `byte[]` 路径逐字节一致，为协议单一真源铺路。
  - **Java 8 强制**：core 用 JDK 8 工具链编译，误用 Java 9+ API 编译失败（ADR-0004）。
  - 新增 **ADR-0016**（反混淆映射策略：锚点有官方映射用 Mojmap、无官方走各 loader 自带）。
- 建立 SDD 规格与治理脚手架：PRD、ARCHITECTURE（含 Mermaid 架构图）、基础 ADR（分层 / 平台抽象 SPI / 多版本适配 / Java8 核心 / 构建复合构建与加载器隔离 / 跨端协议）、API 契约骨架、运维与安全说明。
- 建立防漂移规则集 `.claude/rules/`（架构不变量、范围纪律、验证门、提交规范等）。
- 规划基础跨端网络（跨平台传输 Bukkit/Folia/Sponge/Fabric/Forge/NeoForge + 单人回环 + 务实可靠性层：分片/重组+CRC/重连重同步）+ 进服握手 + 机器码上报 + 机器码封禁 + 融合服（CatServer）适配 + 三层测试（FR-19~FR-25），新增 ADR-0008（融合服 / 活跃平台语义细化），产出 `docs/specs/network-handshake-machine-code-ban.md`。
- 规划初期基础示例（平台能力三件套 / 跨端消息 HUD / 会话心跳，FR-26~FR-28），产出 `docs/specs/foundational-examples.md`。
- 明确命令框架策略（ADR-0009）：各平台用**各自原生命令框架**（Bukkit/Paper/Sponge 原生、Fabric/Forge/NeoForge Brigadier，**不引入 TabooLib**），命令入口在 L3、执行逻辑抽到共享 L0/L1（L2 仅薄 CommandRegistrar 接缝）。
- 据架构可行性评审修订设计：新增 **ADR-0012**（打包 / relocation）、**ADR-0013**（线程归属调度——Folia 无主线程 → SchedulerPort `runForEntity`/`runForLocation`/`runGlobal`）；机器码降级为"**弱客户端标识**"（可伪造 / 可缺席）；握手 / 封禁统一"**进服后即踢**"（Bukkit 插件消息仅 PLAY 阶段）；跨端互通明确"**双端均装我方组件**"前提 + 能力探测降级；网络注册管线随版本纳入 **L4**；**NeoForge 锚点 1.20.2**（无 1.20.1）；测试按平台族（GameTest 仅 mod 加载器）；Java 8 强制 `--release`/animal-sniffer；P1 实施顺序先 Paper+Fabric+Forge；客户端"写一次"价值据实下调。
- 明确 **MVP 验收门 = 自动化测试两套**：①模拟服 GameTest 套件（mod 加载器单人/集成 headless，`gradle runGameTest`，in-process 回环自动跑）②**realserver 套件**（真实专用服，**服务端驱动、客户端验证、单一权威报告**：等待程序化客户端进入 → 触发场景 → 客户端与服务端双重断言 → 客户端回报、服务端聚合 `RESULT PASS|FAIL`、Gradle 门禁，**镜像 AllinCore-New ADR-0020 → 本项目新增 ADR-0014**；测试控制协议仅 test 作用域不入产品协议）——均须完成并通过方算 MVP 交付；Bukkit 家族/Sponge（无 GameTest）以 MockBukkit + 真实服手测达同等覆盖（FR-23 / PRD §6）。
- 新增 **ADR-0015**（功能域组织与拆分约定）：域 = `core-domain` 内的包、只依赖内核 + EventBus、core-runtime 注册、**够大再提升为独立模块、不预建空域**（轻量约定，区别于 AllinCore-New 的 per-domain 重结构）；同步 ARCHITECTURE L0 内部结构、scope-discipline §3。
- 规划平台无关配置与资源路径共享模块（ADR-0010 / FR-29、FR-30）：core-config（YAML/JSON 加载）+ core-paths（预设目录 / 资源位置）+ DataDirectoryPort（平台提供基目录），客户端 / 服务端共用。
- 校正最新版本号为 **26.2**（MC 新版号方案、无 `1.` 前缀，版本模块 `v26_2`）；新增 **ADR-0011**（自有 EventBus 作域间转发解耦、功能域间禁止直接 / 循环依赖，FR-31）；线程模型补充**客户端渲染线程**与 netty 网络线程的线程安全要求。
- 落地 **Forge realserver 验收 harness（编译级）**（FR-23 推进，ADR-0014）：Forge dev run 因 FG6/FML 模块层不向 mod 暴露库依赖而不可用（已证），故 Forge 与 Bukkit 同走 realserver——platform-forge 新增独立 `acceptance` 源集（不入产品 jar、单独打 `mpmt-acceptance-forge` mod，仅验收运行期放入服务端 mods/，shade acceptance/protocol/core-domain 后 reobf 到 SRG）：`ForgeServerGameTestContext`（`MinecraftServer.execute` 切主线程）+ `ForgeAcceptanceControlChannel`（**必须用 `EventNetworkChannel` 裸字节**收发控制通道 `mpmt-test:acceptance` ↔ 复用平台无关 `AcceptanceClient`——绝不能用 `SimpleChannel`，其消息索引帧字节与 Fabric 验收伴侣裸字节控制协议不兼容）+ `MpmtForgeAcceptanceMod`（独立 `@Mod("mpmt_acceptance")`，`-Dmpmt.acceptance=true` 激活 / `ServerStartedEvent` 后 `ServiceLoader` 发现场景 / 驱动线程跑 `ServerGameTestRunner` / 写单一权威报告 / 看门狗绝对截止 + CAS 单次收尾 + 硬退）+ `ForgeSmokeServerScenario`（**不重注册产品通道 `mpmt:main`**，直接裸 `ClientboundCustomPayloadPacket` 发 ACTIONBAR HUD「验收HUD」，文本与 Fabric `SmokeClientVerifier` 期望一致）；平台无关 acceptance 核心原样复用。**验证（编译级）**：`compileAcceptanceJava` + `acceptanceJar` + `reobfAcceptanceJar` 全绿、产品链路（`shadowJar`/`reobfShadowJar`/`verifyPackaging`/`build`）仍通过（产品 jar 381 条目不变、验收 jar 含 mods.toml/services、未泄漏 MC 类）。**真机验证待主控串行跑真实 Forge 专用服 + Fabric 验收伴侣**（沙箱端口受限，不在本任务内）。

### 变更
<对现有功能的改动。>

### 修复
<本版本修复的缺陷。>

### 移除
<被删除的功能。>

> 发版时把"未发布版本"段切成 `## [X.Y.Z] - YYYY-MM-DD`，再新建空的"未发布版本"段。
